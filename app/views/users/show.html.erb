<h1>名前: <%= @user.name %></h1>
<p>メアド:　<%= @user.email %></p>
<% if current_user?(@user)%>
    <p><%= link_to "[編集]", edit_user_path(current_user) %></p>
    <p><%= link_to "[削除]", user_path, method: :delete %></p>
<% end %>
<div><%= link_to "ホーム", root_path %></div>